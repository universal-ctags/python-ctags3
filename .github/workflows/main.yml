# Taken from https://github.com/ssato/python-anyconfig/blob/next/.github/workflows/tests.yml
# https://github.com/ssato/python-anyconfig/blob/next/LICENSE.MIT
name: make

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ${{ matrix.platform }}
    strategy:
      max-parallel: 5
      matrix:
        platform:
          - ubuntu-latest
        python-version:
          - 3.6
          - 3.7
          - 3.8
          - 3.9
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: update package information
        run: sudo apt-get -y -o APT::Immediate-Configure=false update
      - name: install tools and libraries
        run: sudo apt-get -y -o APT::Immediate-Configure=false install make
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tox tox-gh-actions cython
      - name: Test with tox
        run: tox
        env:
          PLATFORM: ${{ matrix.platform }}        
